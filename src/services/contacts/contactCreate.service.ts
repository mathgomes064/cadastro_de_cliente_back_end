import { AppDataSource } from "../../data-source"
import {IContactCreate} from "../../interfaces/contacts/index"

const clientCreateService = async({name, email, telefone}: IContactCreate) =>{
    // const contactRepository = AppDataSource.getRepository()
}